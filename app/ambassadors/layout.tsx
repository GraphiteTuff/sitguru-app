import type { ReactNode } from "react";

/**
 * Public Ambassadors marketing route — Taco mounts from RouteShell.
 */
export default function AmbassadorsLayout({
  children,
}: {
  children: ReactNode;
}) {
  return <>{children}</>;
}
